/**
 * Resume UI Types
 *
 * ONLY UI-specific configuration types.
 * All domain types come from @profile/api-client (SDK).
 *
 * @see ADR-000X — Backend-driven generic sections
 */

// UI-specific configuration
export * from './config';
export * from './presets';
