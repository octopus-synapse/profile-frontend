/**
 * UI Module
 * Centralizes all UI exports for the mobile app
 *
 * Usage:
 * import { View, Text, palette, designSystem } from "@/ui";
 */

// Export primitives (from profile-ui via primitives.ts)
export * from "./primitives";
