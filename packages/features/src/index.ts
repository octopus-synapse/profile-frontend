/**
 * @profile/features
 * Shared feature hooks for web and mobile applications
 *
 * This package provides platform-agnostic hooks that encapsulate
 * business logic and state management, enabling code sharing
 * between React (Next.js) and React Native (Expo) applications.
 */

// Hooks
export * from "./hooks";
